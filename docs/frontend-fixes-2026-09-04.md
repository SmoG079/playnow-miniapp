# 原生前端迁移前修复记录

日期：2026-09-04。修改范围：原生 miniprogram，以及自由约球所需的既有后端查询和数据库迁移。未进行 uni-app 全量迁移，未改动原型，未部署、上传小程序或连接生产数据库。

## 用户确认的范围

- 删除三个未注册空页：home/search、profile/stats、publish/order-manage，各四个文件；删除 booking/club-detail.json 残留。共13个文件。30个注册页面保持不变。
- 真实微信支付尚未开通。预约确认的临时成功文案及调用路径保留，不宣称已完成真实支付验收。
- 普通登录用户可自由发布约球，无需管理俱乐部。定场约球仍沿用现有俱乐部权限及订场契约。
- 已注册但没有后端能力的聊天、用户主页、报名汇总、分账页面保留明确未开放状态，不扩建业务。

## 已修改

- 发布面板向普通用户开放约球入口；自由约球补齐日期和起止时间控件，移除俱乐部选择对提交按钮的错误限制。
- 校验人数、费用、时间及关联预约；切换俱乐部清理旧预约。上传失败不再静默继续创建，增加提交防重。
- 自由约球列表、详情、我的活动查询改为可空俱乐部外连接；补齐我的活动费用/备注字段和详情图片。
- 登录回跳保留旧 slot_id、多时段、名称和 return_mode；首次完善资料后回原目标；赛事订场回到非Tab页面并恢复时间。
- 支付结果网络错误计入轮询上限；状态展示预计算，替换原生WXML不能直接调用的Page方法。
- 时段网格使用后端实际时间行，以起止时间和分钟数判断连续性与一小时门槛；支持单个60/90/120分钟时段，零价也显示预约入口。
- 手机号授权取消不发送 dev_phone；临时头像上传后保存。退出清理角色、管理列表、预约回传；并发401共享刷新，刷新后业务请求失败不清空有效会话。
- 接通系统通知入口、管理统计入口和场地/时段管理入口；补齐工作台拨号事件。
- 修复刷新配置与Promise返回，搜索/列表/时段过期响应覆盖，新旧排序标签明确选择。
- 多俱乐部赛事按所属俱乐部读取；普通用户不请求管理赛事。管理俱乐部和我的活动不再只取首50/20条。
- 手动改地址清理旧坐标；营业时间、人数费用、定价日期时间及31天包含首尾边界校验；修复picker数值类型判断。
- 报名满员/关闭逻辑拦截；审核入口校验和重复操作保护；评论、报名、订单支付取消等动作防重。
- 修复图片缺失、附件非200/打开失败反馈、图片附件预览；补齐NTRP和活动图片展示。举报、群聊、赛程和更多回复不再显示可用能力假象。
- 增加sitemap，只允许公共页面索引；requiredPrivateInfos与实际chooseLocation调用对齐。

## 数据库注意事项

模型原本已允许 match_posts.club_id 为空，但历史迁移未同步。新增迁移 20260904_free_posts（前置66096adfc14a），只调整可空性，不删除记录。降级时若仍有自由约球记录会拒绝恢复NOT NULL，避免数据损失。

本次没有执行迁移。部署时必须核实实际数据库版本并应用此迁移，自由约球才能在仍保留旧NOT NULL约束的环境中发布；不要仅发布小程序。

## 验证

- `npm run test:miniprogram`：25项回归测试，覆盖30页完整性、事件/资源/语法、普通用户发布、失败分支、回跳、时段选择、轮询、角色清理和管理范围。
- `python backend/tests/test_free_post_queries.py`：3项本地SQLite测试，执行从实际接口提取的查询表达式，验证自由约球列表、我的活动和详情。
- 修改的Python文件通过AST语法检查；git diff --check通过。
- 测试使用微信API模拟和内存数据库，不是微信真机或生产联调。已有完整后端pytest套件未运行（当前Python未安装pytest）。

## 保留与待验收

- 真实支付参数、取消/到账/退款链路和微信商户开通按用户决定暂缓；不改写当前临时成功文案。
- 协议/隐私政策缺少正式内容，需提供后接入；不编造法律文档。
- 聊天、客服、分账、报名汇总、赛程、举报及更多子回复仍不新增后端能力。
- 首页/评论公共信息流仍保留原分页范围；全局筛选和完整加载策略在迁移中需单独验收。
- 比赛接口当前仅持久化venue_id，不支持booking_id字段；本轮修复返回和时间同步，不擅加赛事订单模型。
- 微信开发者工具/iOS/Android真机、实际后端权限、支付/退款规则和数据库迁移执行仍需部署环境验证。

原功能提取是修复前快照。迁移时同时参考本记录；源码修复完成不代表uni-app功能已迁移或生产验收通过。
